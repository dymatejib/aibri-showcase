# Витрина работает на стандартной библиотеке. Для прогона тестов нужен только pytest.
# Ставится и проходит одинаково на macOS, Linux и Windows (Python 3.10+).
pytest>=7.0

# Локальное распознавание ФОТО — зависимость необязательная и только для macOS:
# см. requirements-ocr.txt и раздел «Платформы» в README. Без неё тесты фото-пути
# пропускаются с внятной причиной, а весь остальной код гоняется как обычно.
