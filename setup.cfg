# Тот же предел строки для flake8, что и в ruff.toml: два линтера не должны спорить
# между собой — иначе «чисто» зависит от того, кто запустил.
[flake8]
max-line-length = 100
exclude = .git,__pycache__,.venv,venv,.pytest_cache
