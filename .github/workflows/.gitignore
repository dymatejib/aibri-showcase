# Всё, что появляется от прогона, и ничего больше.
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.venv/
venv/

# Рабочая база: каталог создаётся на лету, тесты и стражи берут свой временный
# (переменная AIBRI_DATA_DIR), так что в репозитории ей делать нечего.
data/
*.db
*.db-wal
*.db-shm

# Спутник распознанного текста рядом с фотографией — кэш, а не фикстура.
*.ztxt

.DS_Store
